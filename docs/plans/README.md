# 実装計画

`task-planner` エージェントが書いた実装計画を置く場所。1 タスク 1 ファイル
（`<topic>.md`）。

計画を残す理由は 2 つ。

- **契約（`docs/api-contract.md`）が変わるかどうかを、実装が始まる前に確定させる**
  ため。3 層を並列実装すると、ここが曖昧なまま進んだときに一番高くつく。
- 後から「なぜこの分割・この順序にしたか」を再調査なしで読み返せるようにするため。

実装が終わった計画は消さずに残す（判断の記録なので、古くなっても価値がある）。
